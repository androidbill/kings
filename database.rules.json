{
  "rules": {
    "rooms": {
      "$code": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['createdAt'])"
      }
    },
    "pulses": {
      "$code": {
        ".read": true,
        ".write": true
      }
    }
  }
}
